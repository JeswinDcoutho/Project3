apiVersion: v1
kind: Service
metadata:
  name: cart-service-bg
  namespace: nodejs-devops
spec:
  type: ClusterIP
  selector:
    app: cart-service
    version: blue
  ports:
    - port: 8003
      targetPort: 8003
