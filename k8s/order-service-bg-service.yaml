apiVersion: v1
kind: Service
metadata:
  name: order-service-bg
  namespace: nodejs-devops
spec:
  type: ClusterIP
  selector:
    app: order-service
    version: blue
  ports:
    - port: 8004
      targetPort: 8004
