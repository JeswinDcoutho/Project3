apiVersion: v1
kind: Service
metadata:
  name: user-service-bg
  namespace: nodejs-devops

spec:
  type: NodePort

  selector:
    app: user-service
    version: blue

  ports:
    - port: 8001
      targetPort: 8001
      nodePort: 30083
