apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service-green
  namespace: nodejs-devops
  labels:
    app: inventory-service
    version: green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
      version: green
  template:
    metadata:
      labels:
        app: inventory-service
        version: green
    spec:
      containers:
        - name: inventory-service
          image: 777000838263.dkr.ecr.ap-south-1.amazonaws.com/ecommerce-inventory-service:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8006
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: ecommerce-db-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ecommerce-db-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  name: ecommerce-config
                  key: POSTGRES_HOST
            - name: POSTGRES_PORT
              valueFrom:
                configMapKeyRef:
                  name: ecommerce-config
                  key: POSTGRES_PORT
            - name: POSTGRES_DB
              value: inventory_db
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 15
            periodSeconds: 20
